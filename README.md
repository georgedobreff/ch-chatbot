Simple chatbot with RAG to help with anything Companies House related.
Uses Next.js and Gemini 2.5 Flash Lite.

Model can be changed by editing `src/app/api/chat/route.ts`. By default it uses the cheapest model that can run the tools - `gemini-2.5-flash-lite`.

